// Static marketing site — prerender every page to static assets served from
// the edge cache (Workers only runs for the lead-form action, added in T6).
export const prerender = true;
export const trailingSlash = 'never';
