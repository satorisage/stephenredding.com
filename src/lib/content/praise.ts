import type { Testimonial } from './types';

// No real testimonials yet — the Hugo source held only a placeholder
// ("Name" / "Testimonial Text"), which is NOT shipped (truthful-claims
// constraint: never present a fabricated testimonial). Add real, attributable
// testimonials here as they come in; the Praise page renders empty-state copy
// while this is empty.
export const testimonials: Testimonial[] = [];
